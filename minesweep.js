const minesweeper = () => {
    const userTable = [
        [" ", " ", " "],
        [" ", " ", " "],
        [" ", " ", " "],
    ]

    const drawTable = (table) => {
        return table
    }

    return drawTable(userTable)
}

module.exports = minesweeper
